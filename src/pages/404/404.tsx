import { Text, View } from "react-native";
import NotFoundStyle from "./ui/404Style";

export default function NotFound() {
  return (
    
      <Text style={NotFoundStyle.textStyle}>404 Not found</Text>
    
  );
}